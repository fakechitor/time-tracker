services:
  db:
    container_name: postgres-db
    image: postgres:17.2
    ports:
      - "5432:5432"
    restart: always
    env_file:
      - .env
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: